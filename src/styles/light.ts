import type { ThemeOptions } from "@mui/material";

const LightTheme: ThemeOptions = {
  palette: {
    mode: "light",
    primary: {
      main: "#222930",
    },
    secondary: {
      main: "#5C6678",
    },
    background: {
      default: "linear-gradient(90deg, #EDEDED 0%, #CCDCFF 100%)",
    },
  },
};

export default LightTheme;
